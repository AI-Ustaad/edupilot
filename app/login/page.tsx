import LoginForm from "./LoginForm";

export const metadata = {
  title: "Login - EduPilot",
  description: "Sign in to EduPilot SaaS",
};

export default function LoginPage() {
  return <LoginForm />;
}
